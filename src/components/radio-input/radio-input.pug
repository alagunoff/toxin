mixin radio-input({ id, title, name, value, checked = false } = {})
  .radio-input
    input.radio-input__field(id = id type="radio" name = name value = value checked = checked)&attributes(attributes)
    label.radio-input__label(for = id)= title
