doctype html
html(lang="ru")
  head
    meta(charset="utf-8")

    block keywords

    block description

    meta(name="viewport" content="width=device-width, initial-scale=1")

    link(rel="apple-touch-icon" sizes="180x180" href = require("@root/favicon/apple-touch-icon.png"))
    link(rel="icon" type="image/png" sizes="32x32" href = require("@root/favicon/favicon-32x32.png"))
    link(rel="icon" type="image/png" sizes="16x16" href = require("@root/favicon/favicon-16x16.png"))
    link(rel="mask-icon" href = require("@root/favicon/safari-pinned-tab.svg") color="#5bbad5")
    meta(name="msapplication-TileColor" content="#2d89ef")
    meta(name="theme-color" content="#ffffff")

    block title

  body
    block content